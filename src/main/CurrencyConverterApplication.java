import controller.CurrencyConverterController;

/**
 * Main class responsible for starting the Currency Converter Application.
 */
public class CurrencyConverterApplication {

    /**
     * Main method to initiate the application.
     */
    public static void main(String[] args) {
        // Start the currency converter controller
        CurrencyConverterController.start();
    }
}
